{
  "projectRoots": [
    "frontend"
  ],
  "detector": {
    "ignoreRules": [],
    "ignoreFiles": [],
    "ignoreValues": [
      {
        "rule": "side-tab",
        "value": "*",
        "files": [
          "frontend/src/pages/admin/ArticleEditor.jsx"
        ],
        "createdAt": "2026-09-02T15:54:00.076Z",
        "reason": "blockquote left-border (tiptap .tiptap blockquote, line 265) is a conventional typographic quote-indent style, not a fake-depth card border; reviewed 2026-09 UI consistency pass"
      },
      {
        "rule": "layout-transition",
        "value": "*",
        "files": [
          "frontend/src/pages/Affinity.jsx"
        ],
        "createdAt": "2026-09-02T15:54:06.580Z",
        "reason": "scanner matched SVG strokeWidth (paint-only property on an <line>), not the CSS layout width property; not a layout-thrash risk. Reviewed 2026-09 UI consistency pass"
      },
      {
        "rule": "side-tab",
        "value": "*",
        "files": [
          "frontend/src/index.css"
        ],
        "createdAt": "2026-09-02T21:01:35.766Z",
        "reason": "prose-nba blockquote left-border (L110) is the same conventional typographic quote-indent style already reviewed/suppressed for ArticleEditor.jsx's tiptap blockquote — not a fake-depth card border. 2026-09 UI consistency pass"
      },
      {
        "rule": "layout-transition",
        "value": "*",
        "files": [
          "frontend/src/game/game.css"
        ],
        "createdAt": "2026-09-02T21:01:43.425Z",
        "reason": "reviewed during 2026-09 UI consistency pass (L327/424/730): all three are small isolated progress/stat-bar-fill or accent-rail elements (g-rr rail height, g-bar-fill/g-progress width), not full-layout animation — negligible reflow cost, consistent with the same pattern already used at PlayerCard.css:547. Converting to transform:scaleX would need transform-origin/border-radius rework for marginal gain; judged not worth the risk. Not user-confirmed, my own risk assessment."
      },
      {
        "rule": "layout-transition",
        "value": "*",
        "files": [
          "frontend/src/components/PlayerCard.css"
        ],
        "createdAt": "2026-09-03T08:30:20.538Z",
        "reason": "L547 .pillar-bar-fill transition:width — small isolated stat-bar-fill, not full-layout animation; negligible reflow cost. Same pattern as game.css g-bar-fill/g-progress (already suppressed there, this entry was referenced but never actually added). Reviewed 2026-09 UI consistency pass, not user-confirmed, my own risk assessment."
      },
      {
        "rule": "design-system-font-size",
        "value": "11.5px",
        "files": [
          "frontend/src/pages/sport-select.css"
        ],
        "createdAt": "2026-09-03T08:46:33.651Z",
        "reason": "RankIt gateway card's description is 11.5px to match the two sport-card descriptions it sits directly above (SportSelect.jsx inline fontSize:11.5). Moving only this one onto the ramp would break the visual consistency the card exists to create. The undocumented 11.5px step is pre-existing in the sibling cards. My own assessment, not user-confirmed."
      },
      {
        "rule": "design-system-color",
        "value": "*",
        "files": [
          "frontend/src/rankit/rankit.css"
        ],
        "createdAt": "2026-09-03T09:00:11.965Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-radius",
        "value": "*",
        "files": [
          "frontend/src/rankit/rankit.css"
        ],
        "createdAt": "2026-09-03T09:00:12.074Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-font-size",
        "value": "*",
        "files": [
          "frontend/src/rankit/rankit.css"
        ],
        "createdAt": "2026-09-03T09:00:12.180Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-color",
        "value": "*",
        "files": [
          "frontend/src/rankit/rankit-next.css"
        ],
        "createdAt": "2026-09-03T09:00:12.281Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-radius",
        "value": "*",
        "files": [
          "frontend/src/rankit/rankit-next.css"
        ],
        "createdAt": "2026-09-03T09:00:12.390Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-font-size",
        "value": "*",
        "files": [
          "frontend/src/rankit/rankit-next.css"
        ],
        "createdAt": "2026-09-03T09:00:12.489Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-color",
        "value": "*",
        "files": [
          "frontend/src/rankit/rankit-v030.css"
        ],
        "createdAt": "2026-09-03T09:00:12.589Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-radius",
        "value": "*",
        "files": [
          "frontend/src/rankit/rankit-v030.css"
        ],
        "createdAt": "2026-09-03T09:00:12.701Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-font-size",
        "value": "*",
        "files": [
          "frontend/src/rankit/rankit-v030.css"
        ],
        "createdAt": "2026-09-03T09:00:12.819Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-color",
        "value": "*",
        "files": [
          "frontend/src/rankit/rankit-filter.css"
        ],
        "createdAt": "2026-09-03T09:00:12.933Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-radius",
        "value": "*",
        "files": [
          "frontend/src/rankit/rankit-filter.css"
        ],
        "createdAt": "2026-09-03T09:00:13.043Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-font-size",
        "value": "*",
        "files": [
          "frontend/src/rankit/rankit-filter.css"
        ],
        "createdAt": "2026-09-03T09:00:13.136Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-color",
        "value": "*",
        "files": [
          "frontend/src/rankit/rankit-mobile.css"
        ],
        "createdAt": "2026-09-03T09:00:13.255Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-radius",
        "value": "*",
        "files": [
          "frontend/src/rankit/rankit-mobile.css"
        ],
        "createdAt": "2026-09-03T09:00:13.364Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-font-size",
        "value": "*",
        "files": [
          "frontend/src/rankit/rankit-mobile.css"
        ],
        "createdAt": "2026-09-03T09:00:13.474Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-color",
        "value": "*",
        "files": [
          "frontend/src/rankit/rankit-motion.css"
        ],
        "createdAt": "2026-09-03T09:00:13.592Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-radius",
        "value": "*",
        "files": [
          "frontend/src/rankit/rankit-motion.css"
        ],
        "createdAt": "2026-09-03T09:00:13.699Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-font-size",
        "value": "*",
        "files": [
          "frontend/src/rankit/rankit-motion.css"
        ],
        "createdAt": "2026-09-03T09:00:13.818Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-color",
        "value": "*",
        "files": [
          "frontend/src/rankit/web/rankit-web.css"
        ],
        "createdAt": "2026-09-03T09:00:13.926Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-radius",
        "value": "*",
        "files": [
          "frontend/src/rankit/web/rankit-web.css"
        ],
        "createdAt": "2026-09-03T09:00:14.044Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-font-size",
        "value": "*",
        "files": [
          "frontend/src/rankit/web/rankit-web.css"
        ],
        "createdAt": "2026-09-03T09:00:14.150Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-color",
        "value": "*",
        "files": [
          "frontend/src/rankit/web/cards.jsx"
        ],
        "createdAt": "2026-09-03T09:00:14.258Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-radius",
        "value": "*",
        "files": [
          "frontend/src/rankit/web/cards.jsx"
        ],
        "createdAt": "2026-09-03T09:00:14.364Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-font-size",
        "value": "*",
        "files": [
          "frontend/src/rankit/web/cards.jsx"
        ],
        "createdAt": "2026-09-03T09:00:14.474Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-color",
        "value": "*",
        "files": [
          "frontend/src/rankit/mockData.js"
        ],
        "createdAt": "2026-09-03T09:00:14.568Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-radius",
        "value": "*",
        "files": [
          "frontend/src/rankit/mockData.js"
        ],
        "createdAt": "2026-09-03T09:00:14.685Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      },
      {
        "rule": "design-system-font-size",
        "value": "*",
        "files": [
          "frontend/src/rankit/mockData.js"
        ],
        "createdAt": "2026-09-03T09:00:14.794Z",
        "reason": "user decided RankIt is a separate design system with its own frontend/src/rankit/DESIGN.md (user: 'rankit icin ayri bir design.md yaz' / 'primaryarch'tan ayristiralim'). The detector reads only the root DESIGN.md sidecar, so every RankIt token reads as off-palette by construction. Scoped to the rankit subtree only; the root system stays enforced everywhere else."
      }
    ]
  }
}
