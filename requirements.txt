nba_api>=1.5
pandas>=2.0
numpy>=1.24
pyarrow>=14.0
openpyxl>=3.1
scikit-learn>=1.3
fastapi>=0.141.1
uvicorn[standard]>=0.27
httpx>=0.27
curl_cffi>=0.7
scipy>=1.11
bcrypt>=4.1,<6          # şifre özetleri (passlib bırakıldı, 2026-09)
PyJWT>=2.9,!=2.10.0     # JWT (python-jose bırakıldı; 2.10.0'da issuer açığı var)
pydantic[email]>=2.0
python-multipart>=0.0.9
slowapi>=0.1.9
nh3>=0.2.14          # makale HTML temizleme (blog XSS koruması)
# NOT: euroleague-api RUNTIME'da gerekmez — sunucu yalnız commit'li parquet'i
# okur. Yalnızca veri çekerken lazım: `pip install euroleague-api` (fetch_euroleague.py).
# requirements'a konulunca build'i uzatıp deploy'u düşürüyordu, o yüzden çıkarıldı.
# Aynı gerekçeyle beautifulsoup4 de burada değildi ama fetch_bref_positions.py
# (Basketball-Reference pozisyon/BPM scrape'i, yalnızca fetch-time) hep import
# ediyordu — hiç requirements'ta olmadığı için sıfırdan kurulumda kırıktı
# (2026-07 modifier denetiminde fark edildi). RUNTIME'da gerekmiyor (api/'de
# hiç import edilmiyor), o yüzden yorum satırıyla ayrı not edildi, ana listeye
# değil: `pip install beautifulsoup4` (fetch_bref_positions.py çalıştırmadan önce).
